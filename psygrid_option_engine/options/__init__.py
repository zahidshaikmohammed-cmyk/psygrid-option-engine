"""Option chain parsing, candidate filtering, and constrained contract
selection (Phases 6-7, not yet implemented). Selection must be a
deliberate optimization/constraint process — never "always ATM" / "highest
OI" / "highest volume" / "nearest strike" / "cheapest" shortcuts. See
docs/ARCHITECTURE.md section 4 and docs/PHASES.md.
"""
