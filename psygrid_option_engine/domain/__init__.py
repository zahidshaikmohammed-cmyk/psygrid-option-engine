"""Canonical internal domain types.

`field.py` (SourcedField) is implemented (Phase 1). `snapshot.py`
(MarketSnapshot) and `timeframe.py` (timeframe enums + aggregation
contracts) are Phase 3 — see docs/PHASES.md.
"""
