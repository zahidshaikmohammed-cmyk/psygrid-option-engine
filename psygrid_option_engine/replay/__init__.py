"""Deterministic snapshot store + replay driver (Phase 11, not yet
implemented). Reuses the exact same decision functions as the live path —
no separate backtest-only logic, no look-ahead. See
docs/ARCHITECTURE.md section 3 and docs/PHASES.md.
"""
